def longest_word (arg1, arg2, arg3):
    longestLen =''
    max_len=0

    for word in longest_word:
        if len(word) > max_len:
            longestLen = len(word)
            longestLen = word
    return  longestLen

longest_word('hi', 'hey', 'hello')
